# practice26S
Demo: create a repo

`.md` files follow the same markdown style that we use in `Quarto`. 

We already pasted in files from the Monty Hall exercise in week 1.

# Show (not evalualte) code
Maybe I want to show off some code:

```
library(tidyverse)
ggplot(mtcars, aes(x = mpg, y = wt)) +
  geom_point()
```
